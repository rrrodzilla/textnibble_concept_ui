#:kivy 2.0.0
#:import C kivy.utils.get_color_from_hex
#:import Order order
#:include order.kv


<ActiveOrdersScreen>:
    name: 'active_orders'
    order_total:
    Label:
        id: lbl
        font_size: 64
        font_name: 'Inter'
        text: f'{root.order_total}'